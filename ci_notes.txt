If the test job fails, the build-docker job will not run because it depends on the test job using "needs: test".

This is important because it prevents broken or untested code from being built and deployed. It ensures that only working and verified code moves forward in the pipeline, improving reliability and stability.